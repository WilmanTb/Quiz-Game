<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.QuizGame" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#015A8C</item>
        <item name="colorPrimaryVariant">#015A8C</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

    </style>

    <style name="MyTextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <!-- border color -->
        <item name="boxStrokeColor">#fff</item>

        <!-- The color of the label when it is collapsed and the text field is active -->
        <item name="hintTextColor">#fff</item>
        <!-- The color of the label in all other text field states (such as resting and disabled) -->
        <item name="android:textColorHint">#fff</item>
        <item name="colorControlActivated">#fff</item>
    </style>

    <style name="MyTextInputLayout2" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <!-- border color -->
        <item name="boxStrokeColor">#015A8C</item>

        <!-- The color of the label when it is collapsed and the text field is active -->
        <item name="hintTextColor">#015A8C</item>
        <!-- The color of the label in all other text field states (such as resting and disabled) -->
        <item name="android:textColorHint">#015A8C</item>
        <item name="colorControlActivated">#015A8C</item>
    </style>

    <style name="Dialoge">
        <item name="android:windowBackground">@drawable/empty_bac</item>
        <item name= "windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

    <style name="spinnerTheme">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>